spring.application.name=BE
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/it40}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Khanh.123.123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

app.security.jwt.secret=${JWT_SECRET:change-me-change-me-change-me-change-me}
app.security.jwt.issuer=${JWT_ISSUER:savytech-it40}
app.security.jwt.ttl-seconds=${JWT_TTL_SECONDS:86400}

app.bootstrap.admin.username=${ADMIN_USERNAME:admin}
app.bootstrap.admin.password=${ADMIN_PASSWORD:admin123}
app.bootstrap.hr.username=${HR_USERNAME:hr}
app.bootstrap.hr.password=${HR_PASSWORD:hr123}

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=55MB
server.tomcat.max-http-form-post-size=50MB
server.tomcat.max-swallow-size=50MB